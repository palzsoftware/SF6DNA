# Phase13 automation log - 2026-08-27 03:22 JST

Target batch: Jamie / Chun-Li / Guile / Kimberly

## Actions
- Checked Supabase `character_content_packages` for the four-character batch.
- Found a state mismatch: Combo records had already been populated in prior Phase13 work, while `combo_status` remained `not_started` for all four characters.
- Updated `character_content_packages.combo_status` from `not_started` to `review` for Jamie, Chun-Li, Guile, and Kimberly.
- Kept `rollout_status` as `in_progress` and did not promote verification/publishing states.
- No `main` branch changes.

## Validation policy
- `reviewed` is not treated as `verified`.
- `draft` is not treated as `published`.
- No speculative frame values, Modern commands, or combo properties were added.

## Next priority
- Continue this same four-character batch.
- Fill Move / Frame / Classic Command gaps from current-source data.
- Add Modern Command only when directly confirmed from official/current source material.
- Recheck package counters after every content insert so package status cannot drift from real data.
